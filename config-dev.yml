server:
  port: 8001
  
spring:
  application:
    name: cloud-payment-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    #msyql5.X版本没有cj
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://121.36.254.199:3306/MSDB?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: vxqbNG89W
    
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.springcloud.entities #是有entity别名类所在包
  
eureka:
  client:
    #表示是否将自己注册进eurekaserver,默认为true
    register-with-eureka: true
    #是否从eurekaserver抓取已有的注册信息，默认为true。集群模式下必须设置为true才能配合ribbon使用负载均衡
    fetch-registry: true
    service-url:   
      #单机模式
      defaultZone: http://localhost:7001/eureka
      #集群模式
      #defaultZone: http://eureka7001.com:7001/eureka,http://eureka7002.com:7002/eureka
  instance:
    instance-id: payment8001 #别名
    prefer-ip-address: true #访问路径显示IP
    #Eureka客户端向服务端发送心跳的时间间隔，单位为秒（默认为30秒
    #lease-renewal-interval-in-seconds: 1
    #Eureka服务端在收到最后一次心跳后等待时间上限，单位为秒（默认为90秒），超时将剔除服务
    #lease-expiration-duration-in-seconds: 2